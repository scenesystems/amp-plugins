/**
 * Effect adapters for the Amp Plugin API.
 *
 * @since 0.1.0
 */

/**
 * The Amp `PluginAPI` as an Effect service.
 *
 * @since 0.1.0
 */
export * as Amp from "./Amp.ts"

/**
 * Plugin runtime construction.
 *
 * @since 0.1.0
 */
export * as Runtime from "./Runtime.ts"

/**
 * Effect-based tool definitions.
 *
 * @since 0.1.0
 */
export * as Tool from "./Tool.ts"

/**
 * Tool failures rendered for the agent.
 *
 * @since 0.1.0
 */
export * as ToolError from "./ToolError.ts"
