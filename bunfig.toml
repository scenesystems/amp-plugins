# Bun configuration for the workspace. https://bun.sh/docs/runtime/bunfig
# Dependency ranges are set per package in package.json (see README "Dependency versions"); `bun.lock` pins installs.

[test]
# `bun test` runs only the distribution smoke test, which loads the built bundle under Bun (the runtime Amp uses).
# Unit and contract tests are Vitest projects (`bun run test`, `bun run test:contract`); see vitest.config.ts.
root = "scripts"
